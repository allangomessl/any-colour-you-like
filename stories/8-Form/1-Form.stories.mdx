import { useState, useRef, useCallback } from 'react'
import { Meta, Story, Preview, Props, Source, Mdx } from '@storybook/addon-docs/blocks'
import { Form, Field, Button, Row, Col } from 'universe-ui'
import moment from 'moment'
import 'universe-ui/integrations/unform'

<Meta title="Form/Integration" component={Field.Text} />

# Integration

<br /><br />

<Preview>
  <Story name="Text">
    {() => {
      const form = useRef()
      const [submitData, setSubmitData] = useState('')
      const handleSubmit = useCallback((data) => {
        setSubmitData(JSON.stringify(data))
        return data
      }, [setSubmitData])
      return (
        <Form ref={form} onSubmit={handleSubmit} onSuccess={console.log}>
          <Field.Text label='Name' name='name' />
          <Field.Select label='Sex' name='sex' items={['M', 'F']} />
          <Field.Date label='Birth Date' name='birthDate' />
          <Form path='address'>
            <Field.Text label='Street' name='street' />
          </Form>
          <Form path='phones[0]'>
            <Field.Masked type='phone' label='Phone 1' name='number' />
          </Form>
          <Row>
            <Button onClick={form?.submit}>Save</Button>
          </Row>
          <Row margin='T4'>
            <code>
              {submitData}
            </code>
          </Row>
        </Form>
      )
    }}
  </Story>
</Preview>


## [Unform](https://unform.dev/)
If you already use it or want to use it, just add it to your project
```tsx
  import 'universe-ui/integrations/unform'
```


## [React Hook Form](https://react-hook-form.com/)
If you already use it or want to use it, just add it to your project
```tsx
  import 'universe-ui/integrations/hook-form'
```
and it's done